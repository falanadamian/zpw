<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" const="text/html;charset=UTF-8"/>
    <title>ChatApp</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
          integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.0.4/socket.io.js"></script>
    <title>Simple Chat App</title>
</head>

<body>
<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark">
    <a class="navbar-brand mr-auto mr-lg-0 font-weight-bold" href="#">ChatApp</a>
    <div class="navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto"></ul>
        <form class="form-inline my-2 my-lg-0">
            <input id="username" class="form-control mr-sm-2" type="text" placeholder="Enter username">
            <button class="btn btn-outline-success my-2 my-sm-0" type="button" id="send_username">Change username
            </button>
        </form>
    </div>
</nav>

<main role="main" class="container">
    <div class="d-flex align-items-center p-3 my-3 bg-secondary rounded shadow-sm">
        <img class="mr-3" src="chat-image.png" alt=""
             width="48" height="48">
        <div class="lh-1">
            <h6 class="mb-0 text-white lh-1">Main room</h6>
        </div>
    </div>

    <div class="my-3 p-3 bg-white rounded shadow-sm">
        <div class="row">
            <div class="col-md-12">
                <div class="input-group">
                    <input id="message" type="text" class="form-control">
                    <span class="input-group-btn">
            <button class="btn btn-warning" id="send_message" type="button">Send message</button>
          </span>
                </div>
            </div>
        </div>
    </div>

    <div class="my-3 p-3 bg-white rounded shadow-sm">
        <h6 class="border-bottom border-gray pb-2 mb-0">Last messages</h6>
        <section id="feedback"></section>
        <div id="messages">
            <% if (!!messagesArray && messagesArray.length) { %>
                <% for (let message of messagesArray) { %>
                    <div class="media text-muted pt-3">
                        <img src="user-image.png"
                             class=user-image alt="User image">
                        <p class="media-body pb-3 mb-0 small lh-sm border-bottom border-gray ml-3">
                            <strong class="d-block text-gray-dark">@<%= message['username'] %></strong>
                            <%= message['message'] %>
                        </p>
                        <small><%= message['sent'] %></small>
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>
</main>

<script src="http://code.jquery.com/jquery-latest.min.js"></script>
<script src="chat.js"></script>
</body>
</html>
